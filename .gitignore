.vercel
node_modules
.DS_Store
*.log

# Supercell API token — never commit, never ship to the browser.
.env
.env.local

# Research corpus: raw third-party transcripts stay local (copyright).
# Derived claims/synthesis ARE tracked.
research/transcripts/
research/raw/
research/candidates.txt
# Liquipedia harvest. CC-BY-SA 3.0 permits redistribution, but share-alike would
# propagate that licence to this repo. Regenerate with research/tools/liquipedia-harvest.js;
# only derived aggregates are tracked, and they must credit Liquipedia.
research/data/
research/meta.txt
research/shortlist.txt

# Crawler corpus — regenerate by running, never commit
data/
crawler/ratelimit.json
crawler/yield.json
